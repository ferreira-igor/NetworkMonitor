name: Build Firmware

on:
  push:
    paths:
      - '**/*.ino'

  workflow_dispatch:

env:
  ARDUINO_CLI_VERSION: 1.3.1
  ESP32_CORE_VERSION: 3.3.11
  BOARD_FQBN: esp32:esp32:lolin_d32

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2
        with:
          version: ${{ env.ARDUINO_CLI_VERSION }}

      - name: Configure ESP32 Core
        run: |
          arduino-cli config add board_manager.additional_urls \
            https://espressif.github.io/arduino-esp32/package_esp32_index.json

          arduino-cli core update-index

      - name: Install ESP32 Core
        run: |
          arduino-cli core install \
            esp32:esp32@${ESP32_CORE_VERSION}

      - name: Install Libraries
        run: |
          arduino-cli lib install "Adafruit BME280 Library@2.3.0"
          arduino-cli lib install "Adafruit BusIO@1.17.4"
          arduino-cli lib install "Adafruit Unified Sensor@1.1.15"
          arduino-cli lib install "Adafruit SGP40 Sensor@1.1.4"
          arduino-cli lib install "LiquidCrystal@1.0.7"

      - name: Show Installed Versions
        run: |
          echo "Arduino CLI:"
          arduino-cli version

          echo ""
          echo "ESP32 Core:"
          arduino-cli core list

          echo ""
          echo "Libraries:"
          arduino-cli lib list

      - name: Detect Sketch
        run: |
          INO_FILE=$(find . -maxdepth 1 -type f -name "*.ino" -print -quit)

          if [ -z "$INO_FILE" ]; then
            echo "ERROR: No .ino file found in the project root."
            exit 1
          fi

          echo "Sketch found: $INO_FILE"

      - name: Compile Firmware
        run: |
          mkdir -p build

          arduino-cli compile \
            --fqbn "$BOARD_FQBN" \
            --output-dir build \
            --export-binaries \
            .

      - name: Verify Firmware
        run: |
          echo "Generated files:"
          ls -lh build/

          BIN_FILE=$(find build -maxdepth 1 -type f -name "*.bin" -print -quit)

          if [ -z "$BIN_FILE" ]; then
            echo "ERROR: .bin firmware was not generated."
            exit 1
          fi

          echo ""
          echo "Firmware generated successfully:"
          echo "$BIN_FILE"

      - name: Upload Firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: build/*.bin
